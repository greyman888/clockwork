name: clockwork
description: "Windows-first internal time tracking app."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  fluent_ui: ^4.15.1
  flutter:
    sdk: flutter
  path: ^1.9.1
  sqflite_common_ffi: ^2.4.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/required_definitions.json
